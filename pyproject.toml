[project]
name = "swe-field-manual"
version = "0.1.0"
description = "MkDocs site for the SWE Field Manual"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]
